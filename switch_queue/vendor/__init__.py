"""
Vendored third-party code.

We only vendor when upstream packages aren't reliably installable via pip
(e.g. broken setup.py, no PyPI release). Each subpackage carries the
original LICENSE and a note in its __init__.py about provenance.
"""
